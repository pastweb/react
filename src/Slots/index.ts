export { isSlot, isTemplate } from './utils';
export { Slots } from './Slots';
export { useSlots } from './useSlots';
export { Template } from './Template';

export type { SlotProps, SlotsProps, TemplateProps } from './types';
